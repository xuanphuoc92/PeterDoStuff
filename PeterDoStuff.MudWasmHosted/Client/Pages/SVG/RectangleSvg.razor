@using PeterDoStuff.Tools.Graphics

<rect x="@Model.X" y="@Model.Y"
      width="@Model.ScaledWidth" height="@Model.ScaledHeight"
      fill-color="@Model.FillColor"
      fill-opacity="@Model.FillOpacity"
      stroke="@Model.StrokeColor"
      stroke-opacity="@Model.StrokeOpacity"
      stroke-width="@Model.ScaledStrokeWidth" />

@code {
    [Parameter]
    public Rectangle Model { get; set; }
}
